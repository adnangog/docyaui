
    <li data-treeitem {{#if_equal item.type "folder"}}data-folder{{/if_equal}} class="context-menu-one" data-name={{item.name}} data-id={{item.id}}>{{item.name}}
        {{#if item.childs}}
        <ul>
        {{#each item.childs}}
        {{> tree item=this}}
        {{/each}}
        </ul>
        {{/if}}
    </li>
