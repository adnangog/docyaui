<nav aria-label="paging">
  <ul class="pagination justify-content-end">
    {{# each paging.items }}
    <li class="page-item {{# if this.isDisabled }}disabled{{/ if }}">
      <a class="page-link" href="{{ this.url }}" tabindex="-1">{{ this.label }}</a>
    </li>
    {{/ each }}
  </ul>
</nav>